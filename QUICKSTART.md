# 🚀 Быстрый старт - За 5 минут к работающему боту

## Шаг 1: Установка (1 мин)

```bash
pip install -r requirements.txt
```

## Шаг 2: Получение токенов (2 мин)

### Telegram Bot Token
1. Откройте [@BotFather](https://t.me/botfather)
2. Отправьте `/newbot`
3. Скопируйте токен

### Replicate API Token
1. Зарегистрируйтесь на [replicate.com](https://replicate.com)
2. Перейдите в [API Tokens](https://replicate.com/account/api-tokens)
3. Создайте токен

## Шаг 3: Конфигурация (1 мин)

Откройте `badge_bot.py` и замените:

```python
TELEGRAM_TOKEN = "ВАШТОКЕН"
REPLICATE_API_TOKEN = "ВАШТОКЕН"
LORA_MODEL = "ваше-имя/название-модели"
```

## Шаг 4: Запуск (1 мин)

```bash
python badge_bot.py
```

---

## ⚡ Супер-быстрый старт с готовой моделью

Если у вас ещё нет обученной LoRA, можете временно использовать базовую Stable Diffusion:

1. В `badge_bot.py` замените функцию `generate_image_with_lora`:

```python
def generate_image_with_lora(object_description: str, user_id: int) -> str:
    output = replicate.run(
        "stability-ai/sdxl:39ed52f2a78e934b3ba6e2a89f5b1c712de7dfea535525255b1aa35c5565e08b",
        input={
            "prompt": f"samurai warrior badge, character holding {object_description}, cartoon style, white background",
            "negative_prompt": NEGATIVE_PROMPT,
        }
    )
    return output[0]
```

2. Запустите бота - он будет работать с базовой моделью

3. Позже обучите свою LoRA и обновите код

---

## 📝 Чек-лист перед запуском

- [ ] Установлены зависимости
- [ ] Получен Telegram токен
- [ ] Получен Replicate токен
- [ ] Токены добавлены в код
- [ ] (Опционально) Обучена LoRA модель
- [ ] Бот запущен
- [ ] Протестирован в Telegram

---

## 🎯 Первый тест

После запуска бота:

1. Найдите бота в Telegram (по username)
2. Отправьте `/start`
3. Напишите "magnifying glass"
4. Укажите текст "TEST BADGE"
5. Дождитесь результата (10-30 сек)

Если всё работает - поздравляю! 🎉

---

## 🆘 Проблемы?

### "ModuleNotFoundError"
```bash
pip install -r requirements.txt
```

### "Invalid token"
Проверьте токены в `badge_bot.py`

### "Model not found"
Либо обучите LoRA, либо используйте базовую модель (см. выше)

### Бот не отвечает
```bash
# Проверьте логи
python badge_bot.py

# Или запустите с подробными логами
python badge_bot.py --debug
```

---

## 📚 Дополнительно

- Полная документация: `README.md`
- Обучение LoRA: `python train_lora.py`
- Тестирование модели: `python test_lora.py`

---

**Время на настройку: ~5 минут**  
**Время на обучение LoRA: ~30-60 минут**  
**Стоимость обучения: ~$2-5**

Удачи! 🚀
