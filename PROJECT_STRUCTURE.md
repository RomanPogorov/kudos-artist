# 📦 Структура проекта Badge Bot

```
badge-bot/
│
├── 🤖 Основные файлы
│   ├── badge_bot.py          # Главный файл Telegram бота
│   ├── train_lora.py         # Скрипт для обучения LoRA
│   ├── test_lora.py          # Скрипт для тестирования модели
│   └── requirements.txt      # Python зависимости
│
├── 📖 Документация
│   ├── README.md             # Полная документация (15+ страниц)
│   ├── QUICKSTART.md         # Быстрый старт за 5 минут
│   └── .env.example          # Пример конфигурации
│
├── 🐳 Docker
│   ├── Dockerfile            # Образ для контейнера
│   └── docker-compose.yml    # Оркестрация сервисов
│
├── 📁 Рабочие папки
│   ├── training_images/      # Изображения для обучения LoRA (15-25 шт)
│   ├── test_results/         # Результаты тестовых генераций
│   ├── logs/                 # Логи бота
│   └── fonts/                # Кастомные шрифты (опционально)
│
└── 🔧 Конфигурация
    └── .env                  # Ваши токены (создайте из .env.example)
```

## 🎯 Быстрый старт

### Вариант 1: Локально (рекомендуется для начала)

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Настройте badge_bot.py
#    - TELEGRAM_TOKEN
#    - REPLICATE_API_TOKEN
#    - LORA_MODEL

# 3. Запустите
python badge_bot.py
```

### Вариант 2: Docker

```bash
# 1. Создайте .env из примера
cp .env.example .env

# 2. Заполните .env своими токенами

# 3. Запустите через Docker Compose
docker-compose up -d

# 4. Проверьте логи
docker-compose logs -f badge-bot
```

### Вариант 3: С обучением LoRA

```bash
# 1. Положите 15-25 изображений в training_images/

# 2. Запустите обучение
python train_lora.py

# 3. Выберите опцию 5 (Быстрый старт)

# 4. Дождитесь окончания (30-60 мин)

# 5. Обновите LORA_MODEL в badge_bot.py

# 6. Запустите бота
python badge_bot.py
```

## 📊 Что делает каждый файл

### badge_bot.py (главный файл)
- ✅ Принимает сообщения от пользователей
- ✅ Генерирует изображения через LoRA
- ✅ Добавляет текст на баннер
- ✅ Отправляет готовые бейджи
- ✅ Логирование всех действий

**Основные функции:**
- `generate_image_with_lora()` - генерация через Replicate
- `add_text_to_badge()` - добавление текста на картинку
- `handle_object_input()` - обработка описания объекта
- `handle_badge_text_input()` - финализация бейджа

### train_lora.py (обучение модели)
- ✅ Проверка обучающих изображений
- ✅ Создание ZIP архива
- ✅ Загрузка на Replicate
- ✅ Запуск обучения
- ✅ Проверка статуса

**Интерактивное меню:**
1. Проверить изображения
2. Создать архив
3. Запустить обучение
4. Проверить статус
5. Быстрый старт (всё автоматически)

### test_lora.py (тестирование)
- ✅ Автоматический тест (5 промптов)
- ✅ Интерактивный режим
- ✅ Быстрая проверка
- ✅ Сохранение результатов

**Использование:**
```bash
python test_lora.py
# Выбираете режим тестирования
# Проверяете результаты в test_results/
```

## 🔑 Необходимые токены

### 1. Telegram Bot Token
- Где получить: [@BotFather](https://t.me/botfather)
- Команда: `/newbot`
- Формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

### 2. Replicate API Token
- Где получить: [replicate.com/account/api-tokens](https://replicate.com/account/api-tokens)
- Формат: `r8_xxxxxxxxxxxxxxxxxxxxx`

### 3. LoRA Model Name (после обучения)
- Формат: `your-username/model-name`
- Пример: `john-doe/samurai-badge-lora`

## 💰 Стоимость

| Операция | Стоимость | Время |
|----------|-----------|-------|
| Обучение LoRA | $2-5 | 30-60 мин |
| 1 генерация | $0.005-0.01 | 5-10 сек |
| 100 генераций | $0.50-1.00 | - |
| 1000 генераций | $5-10 | - |

**💡 Совет:** LoRA окупается уже после 200-300 генераций

## 📈 Масштабирование

### Малая нагрузка (<100 запросов/день)
```bash
python badge_bot.py  # Локально
```

### Средняя нагрузка (<1000 запросов/день)
```bash
docker-compose up -d  # Docker + Redis
```

### Высокая нагрузка (>1000 запросов/день)
- Используйте webhook вместо polling
- Добавьте Celery для очереди
- Настройте балансировку нагрузки
- Используйте CDN для изображений

## 🔧 Настройка

### Изменение стиля текста

В `badge_bot.py`:

```python
FONT_SIZE = 48               # Размер шрифта
TEXT_COLOR = "#4A3728"       # Цвет текста
TEXT_STROKE_COLOR = "#F4C542"# Обводка
BANNER_COLOR = "#F4C542"     # Фон баннера
```

### Изменение промпта

```python
BASE_PROMPT_TEMPLATE = (
    f"{TRIGGER_WORD}, "
    f"samurai warrior badge, "
    f"character holding {{object}}, "
    f"cartoon illustration, "  # Измените стиль здесь
    f"flat colors, "
    f"white background"
)
```

### Добавление новых команд

```python
async def my_command(update, context):
    await update.message.reply_text("Привет!")

# Регистрация
application.add_handler(CommandHandler("mycommand", my_command))
```

## 🎨 Примеры использования

### Пользователь пишет:
```
"telescope"
```

### Бот генерирует:
1. Промпт: "aidbox_samurai_style, samurai warrior badge, character holding telescope, ..."
2. Отправляет в Replicate API
3. Получает изображение
4. Добавляет текст "DEBUG NINJA" на баннер
5. Отправляет пользователю

### Весь процесс: 10-30 секунд

## 🆘 Частые проблемы

### Проблема 1: "Module not found"
```bash
pip install -r requirements.txt
```

### Проблема 2: "Invalid token"
- Проверьте токены в `badge_bot.py`
- Убедитесь, что нет лишних пробелов

### Проблема 3: "Model not found"
- Дождитесь окончания обучения LoRA
- Или используйте базовую модель SDXL

### Проблема 4: Медленная генерация
- Уменьшите `num_inference_steps` с 30 до 20
- Используйте более быструю модель (Flux)

### Проблема 5: Текст не помещается
- Уменьшите `FONT_SIZE`
- Ограничьте длину текста до 15 символов

## 📞 Поддержка

- 📖 Полная документация: `README.md`
- ⚡ Быстрый старт: `QUICKSTART.md`
- 🐛 Проблемы: Проверьте логи `logs/bot.log`

## ✅ Чек-лист развёртывания

- [ ] Установлены зависимости (`requirements.txt`)
- [ ] Получен Telegram Bot Token
- [ ] Получен Replicate API Token
- [ ] Подготовлены изображения для обучения
- [ ] Обучена LoRA модель (или используется базовая)
- [ ] Обновлены токены в коде
- [ ] Протестирована генерация
- [ ] Бот запущен и работает
- [ ] Проверена работа в Telegram

## 🎉 Готово к работе!

После выполнения всех шагов у вас будет:
- ✅ Работающий Telegram бот
- ✅ Автоматическая генерация бейджей
- ✅ Уникальный стиль через LoRA
- ✅ Кастомизация текста
- ✅ Логирование и мониторинг

**Время на настройку:** 5-10 минут  
**Время на обучение LoRA:** 30-60 минут  
**Результат:** Полностью автоматизированный бот 🚀
